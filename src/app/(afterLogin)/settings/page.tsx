import AdminUserChart from '@/app/(afterLogin)/settings/components/adminUserChart';

const Settings = () => {
  return (
    <div>
      <AdminUserChart />
    </div>
  );
};

export default Settings;
